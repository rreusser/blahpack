{{alias}}( id, N, d, stride )
    Sorts numbers in increasing or decreasing order.

    Parameters
    ----------
    id: number
        `id`.

    N: integer
        number of columns.

    d: number
        `d`.

    stride: integer
        `stride`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( id, N, d, stride, offset )
    Sorts numbers in increasing or decreasing order, using alternative
    indexing semantics.

    Parameters
    ----------
    id: number
        `id`.

    N: integer
        number of columns.

    d: number
        `d`.

    stride: integer
        `stride`.

    offset: integer
        `offset`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
