{{alias}}( N, cx, strideX, cy, strideY, c, s )
    Performs a linear transformation of a pair of complex vectors.

    Parameters
    ----------
    N: integer
        number of columns.

    cx: number
        `cx`.

    strideX: integer
        stride length for `X`.

    cy: number
        `cy`.

    strideY: integer
        stride length for `Y`.

    c: number
        `c`.

    s: number
        `s`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples


{{alias}}.ndarray( N, cx, strideX, offsetX, cy, strideY, offsetY, c, s )
    Performs a linear transformation of a pair of complex vectors, using
    alternative indexing semantics.

    Parameters
    ----------
    N: integer
        number of columns.

    cx: number
        `cx`.

    strideX: integer
        stride length for `X`.

    offsetX: integer
        starting index for `X`.

    cy: number
        `cy`.

    strideY: integer
        stride length for `Y`.

    offsetY: integer
        starting index for `Y`.

    c: number
        `c`.

    s: number
        `s`.

    Returns
    -------
    out: Float64Array
        Result.

    Examples
    --------
    // TODO: Add examples

    See Also
    --------
