

// MODULES //

import base from './base.js';


// MAIN //

/**
* @license Apache-2.0
*
* @param {number} re - re
* @param {number} im - im
* @returns {integer} info status code
*/
function zsyrfs( re, im ) {
	return base( re, im );
}


// EXPORTS //

export default zsyrfs;
